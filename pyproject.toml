[tool.black]
line-length = 88
include = '\.pyi?$'
extend-exclude = '/(\.git|\.venv|build|dist)/'

[tool.isort]
profile = "black"
src_paths = ["src", "."]

[tool.mypy]
python_version = "3.10"
warn_return_any = true
warn_unused_configs = true
files = ["src", "main.py"]